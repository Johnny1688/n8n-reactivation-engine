Input:
- `reactivation_ai_payload`
- `conversation_core`
- `conversation`
- `customer_name` if present
- `project_key` if present

From payload, use:
- `timeline`
- `last_signal`
- `stop_point`
- `constraints`
- `decision`

--------------------------------
EXECUTION ORDER
--------------------------------

Silently do this in order:
1. resolve current customer name
2. identify strongest real anchor and exact unresolved object
3. generate one activation message using the system prompt rules
4. confirm name, anchor, and single-trigger fit before returning

Do not output these steps.

--------------------------------
DATA
--------------------------------

Customer name:
{{customer_name}}

Project key:
{{project_key}}

Conversation Core:
{{conversation_core}}

Full Conversation:
{{conversation}}

Reactivation AI Payload:
{{reactivation_ai_payload}}

--------------------------------
OUTPUT
--------------------------------

Return ONLY:
{
  "whatsapp_message": ""
}
